# Copy to android/keystore.properties (do not commit).
# storeFile path is relative to the android/ folder (same folder as this file).
storeFile=release-upload.keystore
storePassword=your-store-password
keyAlias=upload
keyPassword=your-key-password
